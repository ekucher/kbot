replicaCount: 1

image:
  registry: "ghcr.io"
  repository: "ekucher/kbot"
  tag: "v1.0.0-4c1533a"
  pullPolicy: IfNotPresent
  os: linux
  arch: amd64

telegram:
  secretName: kbot-secret
  secretKey: TELE_TOKEN
